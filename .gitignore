# Build artifacts
build/
build-*/
out/
*.o
*.obj
*.a
*.exe
.cache/
compile_commands.json
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Trained weights and training data (downloaded separately - see SETUP.md)
data/
# But DO commit benchmark PGNs (small text files documenting strength runs)
!data/
data/*
!data/.gitkeep
!data/*.pgn
# The 1858-entry Lc0 policy index <-> UCI move map is small (~17 KB) and
# required for the policy net to produce non-uniform priors. Commit it
# alongside the code rather than shipping it via Releases.
!data/lc0_policy_map.txt
.accuracy_cache.json
data/books/
